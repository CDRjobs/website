type Client {
  name: String!
  key: String!
  jobBoardTitle: String
  hasFeaturedJobs: Boolean!
}

type GetClientResult {
  data: Client!
}

type Query {
  getClient(clientKey: String!): GetClientResult!
}